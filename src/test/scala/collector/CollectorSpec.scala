import org.scalatest._
import collector.Collector

class CollectorSpec extends FunSuite with DiagrammedAssertions {
  test("Collector should do something") {
    def collector = Collector
    assert(true)
  }
}
